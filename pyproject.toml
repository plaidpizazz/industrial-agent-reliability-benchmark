[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "industrial-agent-reliability-benchmark"
version = "0.1.0"
description = "Industrial reliability benchmark for multi-step agentic AI systems."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Marcellus" }]
dependencies = [
  "anthropic>=0.72.0",
  "datasets>=4.4.0",
  "langsmith>=0.4.0",
  "pandas>=2.3.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=9.0.0",
]

[tool.setuptools.packages.find]
where = ["evals"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["evals"]
addopts = "-q"
