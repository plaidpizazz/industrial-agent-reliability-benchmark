# Optional: only required for real Claude-backed runs.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5

# Optional: when set, local runs can publish traces/evals to LangSmith.
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=industrial-agent-reliability-benchmark
LANGSMITH_TRACING=true
